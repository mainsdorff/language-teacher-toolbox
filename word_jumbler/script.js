const originalWord = document.querySelector("#input");
const jumbleButton = document.querySelector("#jumble-button");

jumbleButton.addEventListener("click", jumbleWord(originalWord.value));

const jumbleWord = word => {
  let jumbledWord = "";

}